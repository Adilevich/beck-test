// Запрос имени у пользователя
var имя = prompt("Пожалуйста, введите ваше имя:");

// Проверка, было ли введено имя
if (имя !== null && имя !== "") {
  // Вывод приветствия на экран
  alert("Привет, " + имя + "!");
} else {
  // Если имя не было введено
  alert("Вы не ввели имя.");
}
